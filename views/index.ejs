<%- include('partials/header'); -%>

<body>
  <!-- profile section -->
  <div class="container-fluid" id="introduction-section">
    <div class="profile-container">
      <img id="my-image" class="img-fluid" alt="Kebing Liu Profile" src="./images/myself.png">
      <div class="brief-intro">
        <h1 class="name">Hello, I'm Kebing Liu</h1>
        <p> I am a 3A BMath Data Science Student at University of Waterloo. <br>
          I love learning new stuffs and build from my own ideas! <br></p>
      </div>
      <a href="pdfs/Resume.pdf" class="btn btn-outline-light resume-link" target="_blank"> Checkout My Resume </a>
    </div>
  </div>

  <hr>

  <!-- skills section -->
  <div class="container-fluid" id="skills-section">
    <div class="skills-content-container row text-center">
      <div class="frontend-skills align-self-baseline container col col-lg-3 col-sm-6">
        <h2 class="skills-title"> Frontend Skills </h2>
        <% frontendSkills.forEach(function(skill) {%>
        <!-- <div class="skill-container"> -->
          <img src="<%= skill.href %>" class="skills-images" alt=<%= skill %>>
          <p> <%= skill.text %> </p>
        <!-- </div> -->
        <% }); %>
      </div>
    
      <div class="backend-skills align-self-baseline container col col-lg-3 col-sm-6">
        <h2 class="skills-title"> Backtend Skills </h2>
        <% backendSkills.forEach(function(skill) {%>
        <!-- <div class="skill-container"> -->
            <img src="<%= skill.href %>" class="skills-images" alt=<%= skill %>>
            <p> <%= skill.text %> </p>
        <!-- </div> -->
        <% }); %>
      </div>
    
      <div class="machine-learning-skills align-self-baseline container col col-lg-3 col-sm-6">
        <h2 class="skills-title"> Machine Learning Skills </h2>
        <% MLSkills.forEach(function(skill) {%>
        <!-- <div class="skill-container"> -->
            <img src="<%= skill.href %>" class="skills-images" alt=<%= skill %>>
            <p> <%= skill.text %> </p>
        <!-- </div> -->
        <% }); %>
      </div>
    
      <div class="other-skills align-self-baseline container col col-lg-3 col-sm-6">
        <h2 class="skills-title"> Other Skills </h2>
        <% otherSkills.forEach(function(skill) {%>
        <!-- <div class="skill-container"> -->
            <img src="<%= skill.href %>" class="skills-images" alt=<%= skill %>>
            <p> <%= skill.text %> </p>
        <!-- </div> -->
        <% }); %>
      </div>
    </div>
  </div>

  <hr>

  <!-- projects section  -->
  <div class="container-fluid" id="projects-section">
    <div class="row">
      <div id="project-carousel" class="carousel slide col col-lg-6 col-md-7 col-sm-8" 
      data-bs-ride="carousel" data-bs-interval="8000">

        <!-- bottom indicators -->
        <div class="carousel-indicators">
          <% projects.forEach(function(project, index) {%>
            <button data-bs-target="#project-carousel" data-bs-slide-to="<%= index %>" aria-label="Slide <%= index %>"></button>
          <% }); %> 
        </div>

        <div class="carousel-inner container">
          <!-- slides -->
          <% projects.forEach(function(project) {%>
            <div class="carousel-item project-item container-fluid">
              <img src="<%= project.imagePath %>" class="carousel-img d-block w-100">
              <h5> <%= project.title %> </h5>
              <p> <%= project.description %> </p>
              <div class="d-flex justify-content-around">
                <a class="project-btn btn btn-outline-light" href="<%= project.github %>" target="_blank"> Github </a>   
                <a class="project-btn btn btn-outline-light" href="<%= project.demoLink %>" target="_blank"> Site </a> 
              </div>         
            </div>
          <% }); %>  
        </div>
      </div>
      
      <!-- project title -->
      <div class="project-title-container col col-lg-6 col-md-5 col-sm-4">
        <ul class="project-title-list container">
          <% projects.forEach(function(project, index) {%>
            <li data-bs-target="#project-carousel" data-bs-slide-to="<%= index %>"> 
              <span class="project-title"> <%= project.title %>  </span>
              <span class="tech-used"> <%= project.tech %> </span>
            </li>  
          <% }); %>   
        </ul>
      </div>
  
    </div>
  </div>
</body>

<%- include('partials/footer'); -%>
